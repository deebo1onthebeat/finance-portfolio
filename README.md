# 💰 Finance Portfolio API

![Python](https://img.shields.io/badge/python-3.12-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109.0-009688.svg)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-336791.svg)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED.svg)

Серьезный пет-проект: **REST API для управления личными финансами**.
Архитектура построена по принципам **Clean Architecture** с разделением на слои (Router, Service, DAO).

## 🛠 Технологии и Стек

Проект написан с использованием современного стека (образца 2025 года):

- **Python 3.12** — Основной язык.
- **FastAPI** — Асинхронный веб-фреймворк.
- **PostgreSQL** — Основная база данных.
- **SQLAlchemy 2.0** — ORM для работы с БД (используется асинхронный драйвер `asyncpg`).
- **Alembic** — Инструмент для миграций базы данных.
- **Pydantic V2** — Валидация данных и settings management.
- **Docker & Docker Compose** — Контейнеризация приложения и БД.

---

## 🚀 Как запустить проект

### 1. Клонирование и зависимости
Убедитесь, что у вас установлен **Poetry**.

```bash
# Установите зависимости
poetry install
```

### 2. Настройка окружения
Создайте файл `.env` на основе примера.
В корне проекта лежит файл `.env.example`. Скопируйте его и переименуйте в `.env`.

### 3. Запуск Базы Данных
Мы используем Docker для запуска PostgreSQL. Убедитесь, что Docker Desktop запущен.

```bash
docker-compose up -d
```

### 4. Применение миграций
Создаем таблицы в базе данных:

```bash
alembic upgrade head
```

### 5. Запуск сервера
Запускаем API в режиме разработки (с авто-перезагрузкой):

```bash
uvicorn app.main:app --reload
```

После запуска документация (Swagger) будет доступна по адресу:  
👉 **http://127.0.0.1:8000/docs**

---

## 📂 Структура проекта

```text
finance-portfolio/
├── app/
│   ├── dao/          # Data Access Object (Работа с БД)
│   ├── models/       # SQLAlchemy модели (Таблицы)
│   ├── routers/      # Эндпоинты (Ручки API)
│   ├── schemas/      # Pydantic схемы (Валидация)
│   ├── config.py     # Настройки проекта
│   ├── database.py   # Подключение к БД
│   └── main.py       # Точка входа
├── migrations/       # Миграции Alembic
├── docker-compose.yml
├── pyproject.toml
└── README.md
```